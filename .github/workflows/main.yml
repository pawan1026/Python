name: test
on: push
jobs:
  clone:
    runs-on: ubuntu-latest
    steps: 
    - name: Checkout
      uses: actions/checkout@v2
    - name: Push to GitHub
      run: |
        ls -al
        cd Packages
        python3 -m pytest testing.py
